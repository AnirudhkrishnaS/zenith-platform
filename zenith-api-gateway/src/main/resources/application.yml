spring:
  application:
    name: zenith-api-gateway

server:
  port: 8080

# JWT (must match user-service secret to validate tokens)
jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-change-in-production-must-be-at-least-32-chars}
